# Cloudflare Workers Assets 헤더 규칙
# 해시된 정적 청크/미디어는 파일명이 콘텐츠 해시에 종속 → 내용이 바뀌면 URL이 바뀜.
# 따라서 영구 immutable 캐시로 재검증(304) 왕복마저 제거 → 학교망에서 첫 로딩 가속.
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
